# 个人用户向 · 待改清单（按优先级）

> 来源：通读 `docs/` + `apps/web/` + `apps/api/` 后的产品审查  
> 日期：2026-08-20  
> 执行约定：**实现 agent 改代码**；本清单为验收口径。关联总纲 [`产品方案_v1.md`](./产品方案_v1.md)。  
> **IA 收束**：入口主次见总纲 **§0.9**（默认只聊；材料为辅；导航 ≤4）。下列 P0/P1 命名与开聊项已完成后，**下一优先实现「P1-IA」**。

---

## P0 — 信任、开聊路径、命名清晰（先做）

### P0-1 认真聊：支持「沿用今天的快照直接开聊」
- [x] 当日已有快照时，认真聊主 CTA = **「用今天的记录开聊」**（不必重填）
- [x] 次要入口才是「再看一眼自己 / 更新快照」
- [x] 与现有 `409 quick_check_required` / 当日判定一致（Asia/Shanghai）
- **涉及**：`CoachHome.vue`、`QuickSelfCheck.vue`、必要时 `handler/coach.go`

### P0-2 诉求选择：禁止一点即提交
- [x] 点选诉求只高亮，须点确认按钮才跳转
- [x] CTA 文案与真实去向一致（开始聊 → 会话，禁止默认回 `/home`）
- **涉及**：`NeedSelect.vue`

### P0-3 画像 / 报告「去聊聊」带对场景
- [x] `BigFiveResult`「带着画像去聊聊」勿写死 `job_search`；优先 `primaryNeed` / 建议场景 / 用户自选
- [x] 评估报告（含 `/assessments/:id`）增加 **「带着这份去聊」**，直达疏导（合读三源）
- **涉及**：`BigFiveResult.vue`、`AssessmentReport.vue`、`api.js` / coach create

### P0-4 人设结果一段合写（禁止双刃/硬伤分栏）
- [x] 结果页只展示 title + blurb + **一段 body** + 标签
- [x] 若后端仍返回 edge/shadow，前端合并展示并去掉「双刃」「硬伤」标题
- [x] 与 [`职场大五画像问卷.md`](./职场大五画像问卷.md) §5.3 一致
- **涉及**：`BigFiveResult.vue`；可选 `bigfive/profile.go` 合并 `composePersonaBody`

### P0-5 导航与入口人话化（最小改动）
- [x] 顶栏：`此刻`→更贴近「首页/聊聊」；`状态`→「记录/趋势」；`说明`→「设置」等（可微调文案，勿加入口）
- [x] 「心理评估」入口只进基线；「再测此刻」只进快照——文案勿混称
- **涉及**：`App.vue`、`CoachHome.vue`、相关 CTA

### P0-6 危机升高：硬停常规疏导
- [x] 基线 `crisisLevel=elevated`（及快照极高危若已有规则）：禁止新建/继续正式疏导陪聊
- [x] 展示危机资源（完善 `CrisisHelp` 文案，至少可用国内通用求助指引）+ 可引导预约
- **涉及**：`handler/coach.go`、`prompts.go`、前端疏导入口拦截

---

## P1 — 连续感、隐私、预约、移动（第二批）

### P1-1 会话连续感
- [x] 首页或侧栏：历史会话列表（进入 / 可选删除）
- [x] 接通或暴露已有 `deleteCoachSession` API
- **涉及**：`CoachHome.vue` / 新组件、`handler/coach.go`

### P1-2 首页进度条
- [x] 轻量展示：职场画像 ✓/○、心理评估 ✓/○、今日快照 ✓/○
- [x] 缺项给一句话 CTA，不做成任务 KPI
- **涉及**：`CoachHome.vue`、`/api/auth/me` 已有字段可复用

### P1-3 隐私触点
- [x] 登录页、快照开填前、预约表单各加一行「仅本人可见 / 非诊断」
- [x] Settings 保留完整说明；后续再做导出/注销 API（见 P2）
- **涉及**：`Login.vue`、`QuickSelfCheck.vue`、`CounselingBooking.vue`、`Settings.vue`

### P1-4 预约状态机
- [x] 用户仅能 `requested` / `cancelled`；`confirmed` / `done` 禁止客户端自助改成「已确认」（若当前可 PATCH 则收紧）
- [x] 列表展示「待确认」预期文案，避免死胡同感
- **涉及**：`handler` booking、`CounselingBooking.vue`

### P1-5 移动端收束（最小）
- [ ] 聊天页：对话区为主、composer 易点；侧栏工具可折叠
- [ ] 大五问卷：考虑分页或分步，避免一屏 15 题压顶（可先做分步）
- **涉及**：`CoachSession.vue`、`BigFiveQuiz.vue`、样式

### P1-6 文案对齐「非每日打卡」
- [x] 基线问卷 UI / 结果建议中去掉「先打卡 3 天」等与产品冲突的话术；改为「节点再测此刻」
- **涉及**：`InitialAssessment.vue`、`AssessmentReport.vue`、assessment 分析文案若硬编码

### P1-IA 主路径收束（入口优先级 · 对照 §0.9）
- [x] 顶栏一级 ≤4：建议 **首页 · 我的 · 设置**（可选「更多」）；预约/练习室不与「聊」同权占主航
- [x] 「我的」合并原评估 + 记录心智（三块 + 趋势归②）；勿再拆两个同级顶栏项
- [x] 首页：**唯一主 CTA = 想聊聊**；材料降为「让对话更贴你」弱层级/折叠；禁止三大问卷块并排 + 进度通关感
- [x] 基线仍可从「我的」与材料区到达并更新（不破 §0.7）
- [x] 认真聊快照门禁仍贴主行动（不破 §0.5）
- **涉及**：`App.vue`、`CoachHome.vue`、`MyAssessments.vue`、`Wellbeing.vue`（可内嵌或降级）、路由/导航文案
- **验收**：[`产品方案_v1.md`](./产品方案_v1.md) §0.9.7

---

## P2 — 账号与数据（有真实用户前必做）

### P2-1 登录模型
- [ ] 文档已要求链接/受控进入；代码勿继续开放「仅用户名接管账号」于生产
- [ ] MVP：邀请码/魔法链接 或 临时关闭自助 register；去掉默认用户名 `default` 观感
- **涉及**：`handler/auth.go`、`Login.vue`、对齐方案 §11

### P2-2 导出与注销
- [ ] 用户数据 JSON 导出（显式确认）
- [ ] 账号删除级联：评估、画像、快照、会话、预约等
- **涉及**：新 API + Settings UI

### P2-3 API / 文档路径对齐
- [ ] 清理或隐藏遗留 `checkins` 与快照双轨（产品：快照=唯一短记录）
- [ ] 错误信息不暴露 `127.0.0.1` / 本地脚本路径给终端用户
- **涉及**：`api.js`、wellbeing handler、前端 error toast

### P2-4 提示词收尾
- [x] 危机/评估用户侧模板尽量收到 `internal/prompts`
- [x] Coach 开场文案「两套材料」改为三源表述
- **涉及**：`prompts.go`、`coach/session.go`

---

## 明确本批不做
- 新增大功能模块（新问卷、新垂类）
- 完整多租户 / 计费
- 临床诊断能力
- 大字报级视觉重做（已有体验样板，按样板小步对齐即可）

---

## 验收口诀
个人用户应能：**打开首页只想去聊 → 材料在「我的」一眼三块 → 认真聊可沿用今日快照 → 危机时不被陪聊拖住 → 知道数据只属于自己。**  
（入口主次以 [`产品方案_v1.md`](./产品方案_v1.md) §0.9 为准。）
